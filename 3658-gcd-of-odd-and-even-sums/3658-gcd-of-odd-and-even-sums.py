class Solution:
    def gcdOfOddEvenSums(self, n: int) -> int:
        return n
        # se, so =0, 0
        # for i in range(2*n):
        #     if i %2 == 0:
        #         se+=i
        #     else:
        #         so+=i

        # gcd = 1
        # for i in range(1, so+1):
        #     if se%i==0 and so%i==0:
        #         gcd=i
        # return gcd
            
        